{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "",
  "main": "program.js",
  "directories": {
    "test": "james-allen-all.js"
  },
  "scripts": {
    "test": "mocha-parallel-tests -R xunit --reporter mocha-junit-reporter --timeout 50000 --slow 1000 test/*.spec.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "jest-html-reporter": "^3.0.0",
    "mocha": "^7.1.1",
    "mocha-junit-reporter": "^1.23.3",
    "mocha-parallel-tests": "^2.3.0",
    "mocha.parallel": "^0.15.6"
  },
  "devDependencies": {
    "jest": "^25.2.7",
    "jest-playwright-preset": "^0.1.1",
    "playwright": "^0.9.24"
  }
}
